# CapstoneGitFlow

Para correr main.py se necesita la base de datos en la misma carpeta que este.

Correr main.py debiese bastar para todas las funciones, sin embargo si se desea correr 1 a la vez, se deberá editar el final del archivo para no llamar las funciones que no se desea ejecutar.